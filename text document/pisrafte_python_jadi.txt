lambda  به تابع های یک بار مصرف گفته میشود که کاربرد بالایی دارد
map نقش map در اینجا تابع مورد نظر را در تمامی مقدار ها ثبت میکند
list برای برگشت دادن یک لیست انجام میشود
filter پیدا کردن مقادیری که نسبط به آن طراحی شده است 
'bige' if x>10 else 'small' یکی ازمدل های if نوشتن است
a.sort()
a.sort(key = lambda x: x[1]) از مقدار دوم استفاده میکرد
sorted(list)
a.sort(reveser = True یا False)
generitor برای تابع های def انجام میشود که فرق آن با def خالی در این است که به صورت یک جا در مموری ثبت نمیشود و تابع را قطع نمیکند و تا آخرین مقدار کار خود را ادامه میدهد